import { PrecipitationAmountPipe } from './precipitation-amount.pipe';

describe('PrecipitationAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new PrecipitationAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
